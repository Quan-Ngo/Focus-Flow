
/**
 * FocusFlow Offline:
 * This service is disabled as the application is now configured
 * to run entirely locally without external API dependencies.
 */
export const getDailyMotivation = async (): Promise<string> => {
  return "Focus on the step in front of you.";
};
